-- Реальные аккаунты: новый провайдер email.
-- Отдельная миграция — новое значение enum нельзя использовать в той же транзакции,
-- а Prisma оборачивает файл миграции в транзакцию (см. миграцию 20260702000002_accounts).
ALTER TYPE "AuthProvider" ADD VALUE IF NOT EXISTS 'email';
