-- Реакции на сообщение личики (T10). Аддитивно/nullable — рулетка/старые клиенты не затронуты.
-- Формат: JSON-объект publicId → emoji, напр. {"00042":"❤️"} (одна реакция на юзера, перезапись
-- своей же меняет эмодзи; null-значение стирает запись целиком на клиенте перед PATCH).
ALTER TABLE "Message" ADD COLUMN "reactions" JSONB;
