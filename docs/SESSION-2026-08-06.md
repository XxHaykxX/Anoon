# Сессия 2026-08-06 — волна безопасности

> Читать вместе с [PROJECT-STATUS.md](PROJECT-STATUS.md) и предыдущей
> [SESSION-2026-08-05.md](SESSION-2026-08-05.md).
> **Работа не окончена.** Раздел «Что осталось» внизу — там всё незакрытое.
> Семь упавших E2E разобраны — см. §4.1, набор `real/` полностью зелёный.

Коммиты: `94b8d71`, `f935709`, `a474beb` — в `main`, не запушены.

---

## 1. Что закрыто

Все находки из `SECURITY-FINDINGS.md` (H1, H2, M1–M4, L1–L4) плюс то, что
всплыло при их разборе.

### Заказанное

| Находка | Суть и фикс |
|---|---|
| **H1** | `POST /auth/rest` был без авторизации и торчал в интернет. Ветка `del` брала uid как есть и удаляла привязку Google — жертва навсегда теряла вход, а uid не секрет: имя p2p-топика И ЕСТЬ uid собеседника. Теперь общий секрет со сравнением за постоянное время, путь убран из Caddy (404, не 403 — чтобы не рекламировать), возвращён под лимитер. |
| **H2** | Анон-фаза отдавала настоящий постоянный `#ID` до раскрытия — в `matched`, в статусе, в каждом кадре call/msg:del/peer:left/activity и в теле пуша. Заменено на алиас матча `~K7X2QM` из `crypto/rand`, алфавит без `I`/`O`/`0`/`1`. Настоящий `#ID` только при раскрытии. |
| **M1** | Лимитер в проде был выключен, а включённый обходился: брал ПЕРВЫЙ элемент `X-Forwarded-For`, который шлёт клиент. Теперь последний хоп и только от приватного peer — если companion выставят наружу напрямую, деградация «строго», а не «нараспашку». |
| **M2** | `/auth/rest` был исключён из лимитера, каждый вызов дёргал Google. Вернули. |
| **M3** | `X-Admin-Id`/`X-Admin-Role` брались из запроса — уровней прав фактически один, журнал модерации подделывался. Добавлен attested-режим: HS256-токен оператора (тот же JWT, что админка уже подписывает для своей сессии), `WithValidMethods` закрывает alg-confusion. Legacy-заголовки остались дефолтом, чтобы панель работала без правок. |
| **M4** | WS принимал любой origin. Теперь тот же allowlist, что у CORS — разъехаться не могут. |
| **L1** | Жалоба помечала медиа в ЛЮБОМ топике как «эскалированные», снимая чужую гарантию исчезновения. Добавлена проверка участия. |
| **L2** | `/media` принимал любой топик и любой URL. Allowlist на относительные ссылки Tinode + проверка участия + лимитер. |
| **L3** | Отписка от пушей удаляла по endpoint без привязки к вызывающему. |
| **L4/ENV** | Дефолт `ENV` перевёрнут на `prod`, чтобы процесс без переменной падал закрыто. |

### Найденное сверх задания

**Аудит авторизации** (все 34 роута, 4 типа WS-кадров, ROOT-хендлер, store-методы;
таблица покрытия с отрицательными результатами — в `scratchpad/AUTHZ-SWEEP.md`):

- **S1** — `/push/subscribe` делал `ON CONFLICT (endpoint) DO UPDATE SET user_id`, то есть переезд строки авторизовался владением строкой endpoint. Узнал чужой endpoint — забрал подписку, жертва молча перестаёт получать уведомления. **Переоткрывал вред, только что закрытый L3.** Фикс: доказательство владения ключами `p256dh`/`auth`, что сохраняет кейс общего браузера.
- **S2** — `call:offer` принимал любой `#ID` без проверки отношений, а `#ID` выдаются из последовательности → обзвон всей базы по порядку. Требуется дружба или живой общий матч.
- **S3** — `/roulette/rate` реплеился без ограничений (`rating_sum + $2` без журнала). Таблица `roulette_ratings` с ключом `(match_id, rater_id)`, среднее выводится из неё.
- **S4** — блокировка действовала ТОЛЬКО в матчинге: заблокированный слал заявки в друзья и пуши тому, кто его заблокировал.
- **S5** — алиас оставался резолвимым после конца матча. Решено: `call:offer` и activity требуют живого линка, поздние `hangup`/`ICE` проходят (иначе звонок нельзя завершить).
- **S6** — верификация почты ставила флаг на ТЕКУЩИЙ адрес, а не на тот, для которого выписан токен. Наивная сверка сломала бы Google-аккаунты навсегда (их адрес в `oauth_identities`, а не в `users.email`) — сравнение повторяет COALESCE-резолюцию.
- **S7** — `UserByID` не фильтрует удалённых. **Не чинили намеренно**: на этом держится модерация (бан того, кто удалился). Контракт задокументирован, для рулетки заведён `LiveUserByID`.

**Ложь клиента** — четыре пути утверждали то, чего не было:

1. **Отклонённое раскрытие** рисовалось как «Профили открыты» с выдуманным `#ID` и несуществующим другом в списке.
2. **Забаненному выдавался выдуманный матч** — `enqueue()` падал в `mockMatch()` на любой ошибке, а бан и 429 это *ответы*, не обрыв. Именно то, что бан должен отнимать. Включённый M1 сделал путь 429 достижимым.
3. **Поиск друзей при 429** заполнялся выдуманными людьми с кнопкой «добавить».
4. **Кнопка «Заблокировать»** в карточке раскрытия не блокировала никого, показывая «Собеседник заблокирован».

Правило теперь: мок — это **режим**, а не обработчик ошибок. `CompanionHttpError` (бэкенд ответил и отказал) пробрасывается, мок только при недостижимом бэкенде.

**Раскрытие заклинивало пару.** Отказ возвращал 200, не трогая базу, поэтому
`reveal_by` навсегда оставался на просящем: отказавший не мог попросить сам, а
если бы попросил — первый не смог бы принять. После первого отказа раскрытие
ломалось, и выглядело это как «собеседник замолчал». Теперь отказ очищает
запрос, шлёт `reveal_declined` (без всякой личности), квота — **две просьбы на
человека на матч**, дальше `409 reveal_asks_exhausted`. Состояние раскрытия
доехало в `/roulette/status`, чтобы лечить пропущенный кадр.

**Админка:** два GET-роута доходили до companion без проверки сессии, когда
прокси самоотключался (`ADMIN_BACKEND=companion` + не-api режим) — отдавали
полные списки reports/users/bans неаутентифицированному. Закрыто проверкой в
самих роутах, что покрывает и Supabase-путь.

**Баг в миграции 0010:** бэкфилл алиасов делал `md5()` (hex), а алфавит
исключает `0` и `1` → ~55% строк получали алиас, который сервер сам отвергает,
и звонки в уже открытых анон-чатах молча ломались. Чинится `translate()`.

---

## 2. Миграции и тесты

`0010` алиасы · `0011` рейтинги · `0012` отказ в раскрытии · `0013` квота просьб.
Все применены, проверено в БД.

⚠️ `0012` и `0013` добавляют колонки, которые обязан выбирать **каждый** запрос,
сканирующий матч (`MatchByTopic`, `MatchByPeerAlias`, `CurrentMatchForUser`,
`LiveMatchBetween`). Пропущенный даёт **ошибку сканирования в рантайме, а не
компиляции**. Первое, что сломается при добавлении пятого запроса.

Тестов добавлено 60+, включая регрессию, которая гонит все 11 злоупотребляемых
роутов мимо их burst и ждёт 429 — та самая проверка, что поймала бы
незавёрнутые `/roulette/rate` и `/roulette/reveal`.

---

## 3. Состояние стенда

Пересобрано и поднято: companion (новый образ, миграции применены), фронт
(новая сборка, :3001), Caddy (:8088). `api/health` через прокси отвечает.

Companion при старте объявляет каждую настройку в лог — видно, в каком режиме
поднялся (legacy-заголовки админки, rest-хук выключен пустым секретом,
лимитер локально выключен).

---

## 4. ЧТО ОСТАЛОСЬ — продолжать отсюда

### 4.1. Упавшие E2E — РАЗОБРАНЫ (2026-08-06, вторая половина дня)

Было: **16 прошло, 7 упало, 3 пропущено.**
Стало: **32 прошло, 0 упало, 3 пропущено** (пропуски — три `test.skip` с TODO(QA)).

Ни одно падение не было «тем, что правили в волне»: рулетка, алиасы и
`CurrentMatchForUser` оказались исправны — бэкенд всё это время создавал
корректный матч (`companion roulette: matched #00011 <-> #00012`, строка в
`roulette_matches` со `status='active'`), а `GET /roulette/status` отдавал его
обеим сторонам. Причин было три, и ни одна не в логике матчинга.

**Причина 1 — origin стенда не в dev-дефолте CORS (корневая).**
Локальный стенд обслуживается Caddy на `:8088`, а `defaultDevCORSOrigins`
(`internal/config/config.go`) перечислял только `:3000`/`:3001`. REST этого не
замечал: за Caddy фронт и companion — **один origin**, CORS к ним не
применяется вовсе. А WebSocket проверяет `Origin` всегда → каждый handshake
`/ws` получал **403** (`websocket: request origin not allowed by
Upgrader.CheckOrigin` в логе companion, ровно с момента фикса M4). То есть
весь реалтайм лежал, а REST работал — потому и выглядело как баг рулетки.
Добавлены `http://localhost:8088` и `http://127.0.0.1:8088`; регресс-тест —
`TestDevCORSDefaultsCoverTheCaddyStack`.

**Причина 2 — клиент уходил в mock от ошибки сокета (важнее первой).**
`CompanionClient.connectEvents` имел `sock.onerror = () => { this.mock = true }`.
Ошибка WS не несёт статуса: «отказано по origin» (бэкенд жив) неотличимо от
«никто не слушает». А mock-режим глушит REST-самолечение — `rouletteStatus()`
первой же строкой делает `if (this.mock) return null`, **не отправляя
запроса**. Замер это подтвердил: поллер `AnoonSearching` (раз в 2 с) сделал
ровно **один** запрос за 36 секунд, дальше молчал. Оба пользователя вечно
висели на «Ищем собеседника…», имея готовый матч в базе, и через 4.5 с mock
подсовывал им заявку в друзья от несуществующего человека. Это то же правило,
что выведено в волне: **мок включается по режиму, а не по факту ошибки.**
Теперь mock переключает только REST (`request()` — в обе стороны), а мёртвый
сокет означает лишь «нет реалтайма» → переподключение.

**Причина 3 — набор гонялся параллельно.** `fullyParallel: true` при том, что
все файлы `real/` водят **одни и те же** admin1/admin2 по одному бэкенду:
общая очередь, общий матч, общий p2p-топик, общий список друзей. Отсюда шесть
падений вида `Target page, context or browser has been closed`, исчезнувших при
`--workers=1`. Закреплено в `playwright.config.ts`: `workers: E2E_REAL ? 1 :
undefined` (мок-набор параллелится по-прежнему).

Отдельно — единственный настоящий дефект теста: `receipts.spec.ts:52` брал
`svg.text-read-tick` по всему экрану и падал на strict mode (26 совпадений —
учётки постоянные, галочки прошлых прогонов никуда не делись). Теперь якорь
скоупится на пузырь именно этого сообщения.

Что это значит для прода: причина 1 — только dev-конфиг (прод обязан
перечислять origin'ы явно), а вот причина 2 ударила бы и в проде — любой обрыв
WS переводил живой клиент в режим фабрикации.

### 4.2. Мок-набор E2E несовместим с текущей сборкой

17 падений в `tests/e2e/*.spec.ts` — **не регрессия**: набор требует
`NEXT_PUBLIC_USE_TINODE` НЕ заданным, а `.env.local` ставит `=1`. Все падают в
одной точке (`signInMock` не доходит до «Чатов»). Чтобы гонять мок-набор, нужна
отдельная сборка без флага.

### 4.3. Не проверено живьём

- [ ] Ручной двухпользовательский проход — анон-звонки через алиас (**не покрыты ничем**) и вся админка (**у неё нет ни одного E2E**). Чеклисты готовы: `scratchpad/QA-SECURITY-WAVE.md` (715 строк), `scratchpad/QA-ADMIN.md`, карта экранов `scratchpad/SCREEN-MAP.md`.
- [ ] UX/UI-проход по экранам, затронутым сменой хендла.
- [ ] Хвосты прошлой сессии: свои сообщения после reload, двойной пузырь, медиа-вьювер, витрина.

### 4.4. Известные ограничения, заведённые задачами

- **Раскрытый матч не завершается никогда** — `EndMatch` фильтрует `status='active'`, выход из раскрытого чата не пишет ничего. `/roulette/status` вечно докладывает старую пару как «текущий матч». Симптом закрыт гардом на клиенте (`if (status.reveal === "revealed") return;`, помечен DO NOT REMOVE), но **нативный клиент споткнётся** — гарда у него не будет. Чинить записью ухода из раскрытого чата.
- **Идентичность при `revealed` приезжает, но клиент её не потребляет** — кадр `revealed` заодно продвигает собеседника в друзья, и вылечить одно без другого = чат «Профили открыты» с именем и без строки в друзьях. Делать через `applyRevealed`.
- **Очередь рулетки в памяти** — рестарт companion её молча опустошает, все ищущие висят вечно. Баг уже сегодня, при одном инстансе.
- **`companion.ts`: пять вызовов глотают отказ** — `friendRequest()` (оптимистичное «отправлено»), `friendRespond()` (принял упавшую заявку — считаешь себя другом), `end()`, `friendsList()`/`listBlocks()` (отказ рисуется как «нет друзей»). Каждому нужна видимая индикация ошибки, которой на тех экранах нет.

### 4.5. Тех-долг и документация

- `docs/AUDIT-2026-07-04.md:95-96` — процедура QA недоспецифицирована (нужен и `NEXT_PUBLIC_DATA_MODE=api`).
- `docs/PROJECT-STATUS.md` — «chats/media/broadcast нет endpoint» устарело: нет только у `chats`, и намеренно (юр-вопрос Q7a).
- Split-brain модерации медиа: Files/Gallery читают из companion, действие уходит в Supabase.
- Код ответа при отказе авторизации админки врёт: где-то 400, где-то **502** (читается как «companion лежит»). Правило: **статус врёт, тело говорит правду**.
- `admin/`: 4 pre-existing ошибки линта, нет тестов и раннера.
- `scripts/create-admin.mjs` умеет только `super_admin` — модератора приходится делать руками в Supabase.
- Добавить `go vet -tags integration ./...` в pre-land gate: пакет за build-тегом, обычный `go test` его пропускает, и сломанный вызов прожил там целую волну.

### 4.6. Крупное, не начатое

- Десктопная вёрстка (38 экранов, ноль адаптива).
- Горизонтальное масштабирование — `docs/SCALING-PLAN.md`, ~9 рабочих дней, порядок и находки там.
- Оплаты, пуши для p2p, прод-деплой Hetzner.

### 4.7. Ждёт Hayk (не код)

Ротация секретов, `GOOGLE_CLIENT_ID`, SMTP, деплой, юр-вопрос Q7a.

---

## 5. Как продолжить

```bash
docker compose -f server-stack/docker-compose.yml up -d
cd frontend && npm run build && npm run start -- -p 3001
cd server-stack && ./bin/caddy.exe run --config Caddyfile.phone
```
Приложение — http://localhost:8088/anoon · витрина — http://localhost:8088/
Учётки: `admin1`/`admin1`, `admin2`/`admin2`.

Прогон real-набора (после стенда):
```bash
cd frontend && E2E_REAL=1 BASE_URL=http://localhost:8088 npx playwright test tests/e2e/real
```
Ожидание — **32 passed / 3 skipped**. Воркер принудительно один (см. §4.1),
руками `--workers` не задавать.

Первое действие следующей сессии — §4.3: ручной двухпользовательский проход
(анон-звонки через алиас и админка) по чеклистам в `docs/qa/`. Автотесты своё
уже говорят.
