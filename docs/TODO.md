# Anoon — TODO

Состояние на **2026-08-11 (день)**. Архитектура: [PROJECT-STATUS.md](PROJECT-STATUS.md).
Итоги последних сессий: [SESSION-2026-08-11.md](SESSION-2026-08-11.md),
[SESSION-2026-08-10.md](SESSION-2026-08-10.md). Отложенное: [BACKLOG.md](BACKLOG.md).

## Готово ✅

- **Веб** (`frontend`): все экраны, рулетка и мессенджер на живом Tinode,
  раскрытие → друзья, звонки, медиа, PWA, пуши, тёмная тема одним набором токенов.
- **Сервер** (`server-stack`): self-hosted Tinode v0.25.2 с патчем анонимности,
  companion (Go: рулетка, #ID, друзья, модерация, пуши, биллинг-seam), Caddy, coturn.
- **Админка** (`admin`): все разделы на companion-API, включая **чтение переписок
  целиком** (решение владельца по Q7a) и вложения через прокси companion.
- **Мобильный клиент** (`mobile`, Expo): все экраны веба, общий стор и клиенты —
  логика одна на две платформы.
- **Прод**: живой, бэкапы с проверенным восстановлением, ротация секретов.

## Тесты (все зелёные)

| Набор | Как запускать | Итог |
|---|---|---|
| real (2 браузера, живой бэкенд) | `E2E_REAL=1 BASE_URL=http://127.0.0.1:8088 npx playwright test tests/e2e/real` | 47 passed / 2 skipped |
| mock | отдельная сборка `NEXT_DIST_DIR=.next-mock NEXT_PUBLIC_USE_TINODE=0` | 22 passed |
| админка | `E2E_ADMIN=1 ADMIN_SESSION_SECRET=… npx playwright test --project=admin` | 13 passed |
| мобилка (web-экспорт против стенда) | `E2E_MOBILE=1 npx playwright test --project=mobile` | 19 passed |
| companion | `go test ./...` | зелёный |

## Дальше ⬜

1. **Мобилка на устройстве.** Web-экспорт доказал, что экраны монтируются и вход
   доходит до «Чатов», но keystore, выбор фото, клавиатура и внешний вид — только
   на телефоне. Нужен EAS dev build (аккаунт Expo заводит владелец) или USB-устройство.
2. **Мобилка: чего нет и почему.** Запись голоса и звонки (нет expo-audio /
   react-native-webrtc), вход через Google (нужен expo-auth-session + client id
   под Android/iOS). Кнопки видимо выключены, а не молча мертвы.
3. **История звонков не переживает reload** (#41): запись живёт в сессии. Решение —
   где она живёт: таблица в companion или служебное сообщение в топик.
4. **Анон-чат не переживает reload** — матч рвётся молча (см. память проекта).
5. **Аудит мёртвых кнопок** по вебу не доведён (начат в сессии 2026-08-10).
6. **Выкатить прод**: он на `bbf45c2`, в `main` с тех пор восемь коммитов.

## Ждёт владельца (не наш блок)

- **Платежи** — армянское юрлицо; в коде только seam + sandbox.
- **SMTP** — отложено, поэтому восстановление пароля не работает (осознанный размен).
- **Внешний монитор** `/api/health` — нужен ping-URL с healthchecks.io.
- **Масштабирование** (`SCALING-PLAN.md`) — отложено до трафика.
- **Аккаунты сторов** — пока не заводим, раздача через EAS.
